# AGENTS.md

## 專案目標與完成標準

在兩週內完成使用預訓練模型的無人機影像超解析度（SR）流程。交付須包含：

- 批次讀取指定資料夾的圖片，執行 SR，將結果寫入指定輸出資料夾，保留原始圖片。
- 可檢視的處理結果、效果評估與成功／失敗摘要，能對應回輸入圖片。
- 可建置與執行的 Linux Docker 映像，以及其他人能照做的操作說明，涵蓋環境需求、模型權重取得、建置、資料夾掛載、執行與評估。
- 使用少量具代表性的真實圖片跑通讀取、推論、輸出、評估與 Docker 執行，實際檢查結果；尚未驗證的部分須明確列出，不能宣稱完成。

此目標是專案背景，不代表每次任務都授權實作整套系統。研究、審查與規劃請只交付所要求的結果。

## 優先順序

1. 快速開發、跑得動、順利完成可用的端到端流程，是最高優先；仍須符合明確的正確性與安全要求，保留已正常運作的行為。
2. 採用最小可行、容易理解與除錯的實作，降低本機資源、時間、API 費用與維護成本。
3. 只有上述條件已滿足，才考慮效能與可解釋性優化；須對應使用者要求或已確認的瓶頸，不能自行展開優化工作。

為避免記憶體不足、相容性問題或無法完成處理而必要的調整，屬於讓流程可運行的工作。不得以追求最佳畫質、速度、架構完整度或可解釋性延後基本交付。預設使用現成預訓練權重，不自行擴張為訓練、微調、多模型競賽或研究平台。

## 執行環境

- 專案運行於 **Linux**；本機開發與執行使用 **WSL（目前為 Ubuntu 24.04）**，容器也以 Linux 為目標。
- 目前專案根目錄：`/home/minervamuses/drone-image-analysis`。其他人的部署位置可以不同，程式與 Docker 不得寫死這個本機路徑。
- 每個新任務先做最少量的唯讀檢查，確認專案根目錄、適用指引與實際執行環境，並簡短告知使用者。Shell、路徑、Git、Python 與套件工具須使用一致的 Linux／WSL 環境，沿用專案已有工具鏈。
- 本專案的執行環境以 WSL 為準。即使 Agent 從 Windows 啟動，也應透過 `wsl.exe`（例如 `wsl.exe -- bash -lc "..."`）在 WSL 內執行 Python、Git、套件管理、安裝、建置與測試等專案指令；`\\wsl.localhost` 可用於存取專案檔案，但不得因此改用 Windows 原生的 Python、Git 或套件工具。Agent 執行於 Windows 本身不是停止工作的理由，應優先從 Windows 呼叫 WSL 完成操作。只有在 WSL 本身無法使用，或所需操作確實無法在 WSL 內執行時，才停止相關操作並說明原因，不得自行改用 Windows 原生工具鏈繞過。

## 本機資源限制與提前告知

- 本機 GPU 是 **NVIDIA GeForce RTX 5070 Ti Laptop GPU（筆電版）**。不得套用桌機版規格，也不得假定 VRAM、可用 RAM、磁碟空間或實際速度。
- 在選定需下載或執行的模型、推論框架或大型工作前，以必要的唯讀資訊確認可用資源與 Linux／WSL、GPU 驅動、CUDA／框架相容性；未確認的部分須標明。
- 若已知或合理預期方案無法在本機順利運行，**必須在下載、安裝或啟動相關工作前告知使用者**：限制與證據、未知部分、預估時間／資源／費用，以及最小可行替代方案。不要先開始昂貴嘗試，再回報本機不足。
- 優先考慮現有實作支援的小批次、分塊處理或較輕量模型；是否適用由實際圖片與小樣本結果決定。不得未告知就降低需求、默默改用 CPU 長跑，或改用雲端／付費服務。
- 驗證先用最小代表樣本。全資料集重跑、模型／GPU 掃描、付費呼叫或預估超過約十分鐘的工作，先說明具體成本並取得使用者同意；不要把未知資源需求當成可直接執行。

## 工作方式與範圍

- 先確認本次要求與成功證據，只讀取相關程式、設定、文件與 skills；不要每次都掃描整個專案或讀取全部文件。
- 優先重用現有程式、快取、輸出與測試。做能解決具體問題的最小修改，不順便重構、換框架或處理鄰近問題。
- 在已授權範圍內，自行完成實作、最小必要驗證與直接相關的修正，不為一般可逆操作反覆要求確認；已取得的授權不重複詢問。
- 新增正式依賴、變更套件／環境／lock 檔、公共介面或資料格式、持久化結構、服務或並行模式，以及大型架構調整，若尚未獲授權，須先提出具體最小方案與成本。聚焦修正若需超過三個正式程式檔或新增可避免的持久模組，也先說明必要性。
- 未取得明確授權，不執行 commit、push、merge、rebase、切換分支或修改 worktree。保留使用者既有修改。
- 遇到必須確認的界線，先完成不依賴該決策的準備，提出可審查的具體結果；若因 skill 或文件指引而停止，指出來源與適用條文，不把建議擴張為額外限制。
- 兩次聚焦修正仍失敗，或一次昂貴嘗試無效，就回報已知證據、未解原因與最小下一步；不要自行升級為重寫或更大規模實驗。

## 評估、驗證與回報

- 以真實圖片的輸出為主要證據；程序退出成功或測試通過本身，不足以證明 SR 結果正確。
- 有配對且對齊的高解析度參考圖時，才使用 PSNR／SSIM 等參考式評估；沒有參考圖時，說明限制，提供固定樣本的視覺比較與適用的評估方式，不把放大後的輸入當成真實高解析度答案。
- 交付所需的效果評估不等於開放式畫質優化。不要為了改善分數而擴張模型搜尋，也不要把生成的紋理直接當成真實地物細節。
- 執行與修改直接相關、成本最低的檢查。純文件變更只需確認內容與差異，不跑模型或建立測試框架。只有新變更、失敗或未解疑慮需要時才擴大或重跑測試；不另建測試或基準框架。完整既有測試套件只在相關且便宜時於收尾跑一次，昂貴時先取得同意。
- Docker 與操作說明使用實際驗證過的命令，明列 GPU 執行前提、權重與必要版本；未驗證的步驟不得寫成已確認可用。
- 最後簡短回報修改檔案、實際執行的檢查與結果、未完成或受限部分。區分確認事實與推測；本次要求完成就停止，不自行展開後續改良。

## 指引維護

本檔依 [OpenAI：Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra) 的方向編寫：保留必要背景、完成標準與清楚界線，按任務讀取資料，避免過度規定流程與重複驗證。新增規則前確認其必要性，不複製整套通用手冊或加入尚不存在的檔案與命令。
